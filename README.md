# School Canteen

Веб-приложение для автоматизации учета питания в школьной столовой.
Реализована ролевая модель доступа: Ученик, Повар, Администратор.

## Функционал

### Ученик
- Авторизация (ID/Login/Email).
- Просмотр меню на текущий день.
- Онлайн-заказ питания.
- Оплата или подписка.
- Учет аллергенов (подсветка опасных блюд).
- История заказов.

### Повар
- Выдача питания по идентификатору ученика.
- Управление меню (формирование на дату).
- Складской учет (мониторинг остатков, ручная корректировка).
- Управление номенклатурой (добавление блюд/продуктов).
- Создание заявок на закупку.
- Статистика выдачи за день.

### Администратор
- Статистика (выручка, посещаемость).
- Обработка заявок на закупку.
- Финансовые отчеты.
- Управление пользователями.

## Стек технологий
- **Backend:** Python 3.13, Flask
- **Database:** SQLite
- **Frontend:** HTML/CSS/JS
- **Deploy:** Docker, Docker Compose

---

## 2. Установка и запуск

Самый простой способ запустить проект — использовать Docker.

1.  Скачать проект на ваш ПК или сервер.
2.  Откроть терминал в корневой папке проекта.
3.  Собрать и запустить контейнер:
    ```bash
    docker-compose up -d --build
    ```
4.  **Инициализация базы данных** (необходимо выполнить только при первом запуске):
    ```bash
    docker exec school_canteen python database/init_db.py
    ```
5.  Приложение доступно по адресам в терминале.

---

## 3. Как пользоваться сайтом

### Вход в систему
При первом запуске создаются тестовые учетные записи. Пароль для всех: `1234`.

| Роль | Логин | Email | ID (для входа) |
| :--- | :--- | :--- | :--- |
| **Администратор** | `Admin` | `admin@school.ru` | `00000001` |
| **Повар** | `Cook` | `cook@school.ru` | `00000002` |
| **Ученик** | `Student` | `student@school.ru` | `00000003` |

### Сценарии использования

**Для Ученика:**
1.  Войдите под логином `Student`.
2.  Перейдите в "Настройки предпочтений" и укажите аллергены (например, "Молоко").
3.  Вернитесь в Меню. Блюда с аллергенами будут подсвечены красным.
4.  Нажмите "Заказать" на выбранном блюде.
5.  В разделе "История" вы увидите статус заказа.

**Для Повара:**
1.  Войдите под логином `Cook`.
2.  **Склад:** Проверьте остатки. Если продуктов мало, нажмите "✏️ Изм." и введите новое количество.
3.  **Меню:** Выберите дату и добавьте блюда в расписание на завтрак/обед.
4.  **Выдача:** Когда придет ученик, введите его ID (`3`) в поле поиска. Вы увидите его заказ. Нажмите "Выдать".

**🛡Для Администратора:**
1.  Войдите под логином `Admin`.
2.  В разделе "Закупки" одобрите заявки от повара (склад пополнится автоматически).
3.  В разделе "Статистика" следите за выручкой и количеством выданных порций.

---

## 4. Структура проекта

```text
project/
│
├── Backend/
│   ├── app.py 
│   └── requirements.txt
│
├── Frontend/
│   ├── css/
│   ├── js/
│   ├── assets/
│   └── *.html
│
├── database/
│   ├── init_db.py
│   └── school_canteen.db
│
├── Dockerfile
└── docker-compose.yml
